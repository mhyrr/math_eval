# Phase 4 hold-out row. GENERATED by scripts/mk_survey4.py -- edit the table
# there, not this file. Scored blind against the 2025-08-31 cutoff; the reveal
# snapshot was not read. See memo/PREREGISTRATION.md.
id: e617-graph-theory
area: graph theory
title: "Erdos #617 -- Erdos-Gyarfas: r-colourings of K_(r^2+1) and a missing colour on r+1 vertices"
statement: >
  Erdos-Gyarfas: r-colourings of K_(r^2+1) and a missing colour on r+1 vertices

sources:
  - url: https://github.com/google-deepmind/formal-conjectures/blob/5a60e068/FormalConjectures/ErdosProblems/617.lean
    what: "Lean statement, formal-conjectures at pinned commit 5a60e068. Read via
      scripts/blind.py, which serves the EARLIEST version of the file (post-cutoff; declared leak)
      with the category attribute, every answer(...) payload, post-cutoff
      bibliography entries and outcome prose redacted. The unredacted file
      carries the resolution status and was not read."
    tier: PRIMARY
  - url: https://github.com/teorth/erdosproblems/blob/009173b2/data/problems.yaml
    what: "Cutoff snapshot of the community database, commit 009173b2
      (2025-08-31T23:55:32Z). Source for tags, prize and the `open` state used
      to discharge G0 nominally. Contains no outcome information."
    tier: PRIMARY
  - url: https://www.erdosproblems.com/617
    what: "Canonical problem page. NOT FETCHED -- the site returns 403 to
      automated requests (CLAUDE.md), and it would in any case show the current
      status. Listed as the pointer a reader should follow, not as a source
      consulted."
    tier: PRIMARY

# --- gates ---
open_to_field: true
no_new_theory_required: null
verification_standard_exists: true
g0_checked_rigorously: false

form: UNIVERSAL
prior_attention: LOW

# --- axes ---
certificate_cost: 5
evaluator_availability: 4
formal_library_coverage: 3
reservoir_proximity: 2
witness_representability: 5
independence_risk: 5

formalizable_without_ambiguity: true

axis_rationale: >
  The strongest Route W row that is also cheap. A counterexample is a
  single colouring of a single finite complete graph (D=5) and checking it
  means scanning the (r+1)-subsets -- exact, and trivial for the small r
  where the problem is open (B1=4, A=5). B2=3: Sym2, Fintype and finite
  colourings are mature in mathlib and nothing here is asymptotic.
  attention LOW: the file cites only Erdos-Gyarfas 1999, which settled r=3
  and r=4, and nothing since.

unverifiable_claims:
  - "prior_attention LOW is inferred from a single reference in the Lean
    file. If this problem has in fact been attacked computationally since
    1999, the LOW call is wrong and the conditional Route N count should
    not include it. Route W clearance does not depend on it."
  - "G0 is discharged NOMINALLY, from the cutoff database's `open` state at
    2025-08-31, not by a literature search. That is exactly the list status
    Bloom described -- a statement about the list's coverage, not
    humanity's knowledge -- so g0_checked_rigorously is false and Route N
    cannot clear."
  - "G1 (does resolution require extensive new theory?) is null. It is not
    answerable for an open problem from a redacted Lean file and a status
    database, in either direction, and guessing it would be the whole
    result. Under --optimistic this is the only gate being granted."
  - "Axis C is the least sourceable axis under the blindness protocol.
    Scoring 'is there untried machinery in an adjacent field' properly
    needs a literature survey per problem; these calls are judgements
    against the RUBRIC.md anchors from the references visible in the Lean
    file."
